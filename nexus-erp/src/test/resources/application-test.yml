spring:
  sql:
    init:
      mode: never
  autoconfigure:
    exclude:
      - org.springframework.boot.actuate.autoconfigure.jdbc.DataSourceHealthContributorAutoConfiguration

nexus:
  security:
    jwt:
      secret: test-jwt-secret-0123456789abcdef0123456789abcd
