<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nexus.erp.infrastructure.mapper.ErpStockMapper">

    <!--
      乐观兜底扣减库存：仅当当前库存 qty >= num 时才扣减，避免并发下出现负库存。
      注：表字段实际为 qty（不是 quantity）。
    -->
    <update id="deductStock">
        UPDATE erp_stock
        SET qty = qty - #{num}
        WHERE tenant_id = #{tenantId}
          AND product_id = #{productId}
          AND warehouse_id = #{warehouseId}
          AND del_flag = 0
          AND qty &gt;= #{num}
    </update>

    <update id="upsertIncreaseStock">
        INSERT INTO erp_stock (tenant_id, product_id, warehouse_id, qty, version, del_flag)
        VALUES (#{tenantId}, #{productId}, #{warehouseId}, #{num}, 0, 0)
        ON DUPLICATE KEY UPDATE
            qty = qty + VALUES(qty),
            del_flag = 0
    </update>

</mapper>

